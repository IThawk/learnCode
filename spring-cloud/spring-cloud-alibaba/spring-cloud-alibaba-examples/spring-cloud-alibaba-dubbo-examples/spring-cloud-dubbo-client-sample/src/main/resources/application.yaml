dubbo:
  scan:
    base-packages: com.alibaba.cloud.dubbo
  protocols:
    dubbo:
      port: -1

feign:
  hystrix:
    enabled: true

server:
  port: 8099

management:
  endpoints:
    web:
      exposure:
        include: dubborestmetadata