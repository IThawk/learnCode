dubbo:
  application:
    name: gpmall-user
    owner: Mic
  registry:
    address: localhost:2181
    check: true
    group: dubbo-dev
    simplified: true
  metadata-report:
    address: localhost:2181
    retry-times: 30
    cycle-report: false
    group: dubbo-dev
  scan:
    base-packages: com.gpmall.user

captchaFlag : false