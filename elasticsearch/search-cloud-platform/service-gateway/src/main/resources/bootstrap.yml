spring:
  application:
    name: service-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      config:
     #配置中心文件类型
        file-extension: yml
        server-addr: 192.168.56.101:8848      #配置中心服务地址
     #配置中心编码
        encode: utf-8
